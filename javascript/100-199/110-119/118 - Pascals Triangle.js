/**
 * @param {number} numRows
 * @return {number[][]}
 */
 var generate = function(numRows) {
    let result = [];

    for (var i = 0; i < numRows; i++) {
        let curRow = [];
        if (i = 0) {
            result.curRow.push(1);
        }
    }

    return result;
};