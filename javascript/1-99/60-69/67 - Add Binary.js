/**
 * @param {string} a
 * @param {string} b
 * @return {string}
 */
 var addBinary = function(a, b) {
    let carryover = 0;
    let result = Array(Math.max(a.length, b.length)).fill(0);

    let i = 0
};